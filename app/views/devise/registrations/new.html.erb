<%= title "Sign up" %>
<% if params[:plan] == "1020" || params[:plan] == "2030" || params[:plan] == "3040" %>
	<div class="blue-background">
		<div class="container">	
			<div class="col-md-6 col-md-offset-3 well signin">

					<h2>Request invite</h2>

						<%= simple_form_for(resource, :as => resource_name, 
			                      :url => registration_path(resource_name), :validate => true, 
			                      wrapper_mappings: { boolean: :vertical_boolean }) do |f| %>
					  	<%= f.error_notification %>


					  	<div class="first-name">
			          	  <%= f.input :first_name, required: true, autofocus: true, label: false , placeholder: "First name (required)" %>
			        	</div>

				        <div class="last-name">
				          <%= f.input :last_name, required: true, label: false, placeholder: "Last name (required)" %>
				        </div>
					    <%= f.input :email, required: true, label: false, placeholder: "Email (required)" %>
					    <%= f.input :organisation, required: true, label: false, placeholder: "Company Name" %>
					    <%= f.input :password, required: true, label: false, placeholder: "Password (required)", hint: (" 8 characters minimum" if @minimum_password_length) %>
					    <%= f.input :password_confirmation, label: false, placeholder: "Password confirmation (required)",required: true %>
					    <%= f.hidden_field :plan, { :value => params[:plan]} %>



					  <div class="new-btn">
						<%= f.button :submit, 'Request Invitation', :class => 'btn-primary btn-block btn-lg' %>
					  </div>

					
					
					

				<% end %>
			</div>
			
			<div class="col-md-6 col-md-offset-3 shared">
				<%= render "devise/shared/links" %>
			</div>	

			<div class="col-md-4 col-md-offset-4 well signin">
				<h4>Why is Minibarra invite only?</h4>
				<p>The Minibarra team lives to build the best product we can possibly build. It's our relationship with our users that lets us do this. We want to make sure we're right for you.</p>
				<hr>
				<h4>How long do I need to wait?</h4>
				<p>You'll hear from us within <strong>24 hours</strong>. After your invite is approved, you'll be given an on-boarding date. We'll keep you up-to-date throughout the process.</p>
				<% 
=begin %>
				<hr>
				<h4>What exactly are you looking for?</h4>
				<p>We want to work with customers just like us. If you produce great food and give excellent customer service, you're perfect. </p>
				<hr>
				<h4>How do you come to your decision?</h4>
				<p>Once we recieve your request, our team will do some research on you. It's not a strict process, we'll most likely view your website, your menu, read reviews*. Every evening, we have a meeting and decide who to invite.
					<br>
					<br>
				*(Don't worry — we read the 'critics' reviews on TripAdvisor with a large pinch of salt).</p>
							<hr>
				<h4>What happens if I'm not successful?</h4>
				<p>You'll get an email from us letting you know. We'll tell you why, and ask you if you'd like to be on our launch list (early 2018).</p>
				<% 
=end %>
			</div>
		</div>
	</div>	
<% else %>
	<%= render 'pages/pricing_table.html.erb' %>
<% end %>	