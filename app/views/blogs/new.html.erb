<% if user_signed_in? && current_user.admin == true %>
	<div class="container">
	  <div class="row">
	    <div class="panel">
	    	<h1>New Blog</h1>

				<%= render 'form', blog: @blog %>

				<%= link_to 'Back', blogs_path %>
	    </div>  
	  </div>  
	</div>
<% end %>