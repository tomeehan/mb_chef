<%= simple_form_for @staff, validate: true do |f| %>
  <%= f.error_notification %>
    
      <br>
      <div class="first-name">
         <%= f.input :first_name, required: true, autofocus: true, label: false , placeholder: true %>
      </div>
      <div class="last-name">
         <%= f.input :surname, required: true, label: false, placeholder: true %>
      </div>
      <%= f.input :email, label: false, placeholder: true %>
      <%= f.input :job_title, label: false, placeholder: true %>
      <%= f.input :manager %>

    <div class="new-btn">
      <%= f.button :submit, :class => 'btn-primary btn-block btn-lg' %>
    </div>

<% end %>
